"use client";
import { useState } from "react";
import { GenericScreen } from "../_components/intelligence-workspace";
import { apiConfigured, apiOrigin, creatorChannelId, intelligenceApi } from "@/lib/intelligence-api";
import { toast } from "sonner";
export default function SettingsPage() { const [busy, setBusy] = useState(false); return <GenericScreen title="Settings" eyebrow="Connection & safety"><div className="grid gap-4 md:grid-cols-2"><div className="rounded-2xl border border-white/10 bg-white/[.035] p-5"><h2 className="font-semibold">Frontend configuration</h2><dl className="mt-5 space-y-4 text-sm"><div><dt className="text-slate-500">API origin</dt><dd className="mt-1 break-all text-slate-200">{apiOrigin() || "Not configured"}</dd></div><div><dt className="text-slate-500">Creator channel ID</dt><dd className="mt-1 break-all text-slate-200">{creatorChannelId() || "Not configured"}</dd></div></dl><p className="mt-5 text-xs leading-5 text-slate-500">Backend secrets and provider credentials are never editable or stored in this frontend.</p></div><div className="rounded-2xl border border-white/10 bg-white/[.035] p-5"><h2 className="font-semibold">Backend health</h2><p className="mt-2 text-sm text-slate-400">{apiConfigured() ? "Check the live health endpoint." : "Configure the environment first."}</p><button disabled={!apiConfigured() || busy} onClick={async () => { setBusy(true); try { const r = await intelligenceApi.health(); toast.success(`Backend: ${r.status}`); } catch (e) { toast.error((e as Error).message); } finally { setBusy(false); } }} className="mt-5 rounded-xl bg-cyan-300 px-4 py-2 text-sm font-semibold text-slate-950 disabled:opacity-50">{busy ? "Checking…" : "Check health"}</button>{apiOrigin() && <a className="ml-3 text-sm text-cyan-300" href={`${apiOrigin()}/docs`} target="_blank" rel="noreferrer">Swagger docs ↗</a>}</div></div></GenericScreen> }
